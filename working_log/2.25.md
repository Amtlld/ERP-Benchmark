# 2.25

今天启动对该项目的适配工作，主要需要开展以下几方面的工作：

1. 使用MATLAB处理Benchmark数据集，将其标准化后存储在dataset/200Hz/Benchmark/下
    - 构建特征文件 Feature/feature_{ID}.npy 将第 ID 号测试者的有效脑电 Epochs 打包导出。确保维度为 (样本数, 时间点长度, 通道数)
    - 构建特征文件 Label/label_{ID}.npy 标签矩阵应为 (样本数, 标签属性数量)。通常你需要在其中某几列明确写下每次 trial 对应的类别标签(Target Label)和该受试者的ID。
2. 编写Data Loader代码：需要在 data_provider/dataset_loader/ 下新建一个加载器脚本，可直接复制 pd_sim_loader.py 的代码进行少许魔改，需要实现：
    - 划分数据集方法 `get_id_list_my_dataset`(...)： 读取你的 Label 目录，获取所有参与者的 ID。按照你们的任务需求实现留一法（loso）、交叉验证（mccv）或固定比例（如8:1:1）将所有 ID 拆分为 `train_ids`, `val_ids`, `test_ids`。
    - 实现 dataset 类 `MyDatasetLoader(Dataset)`： 继承 Dataset。在 `__init__` 函数中使用提供的 load_data_by_ids 函数导入对应 ID 下的 .npy 数据，并调用 normalize_batch_ts 做归一化。 重点在于提取分类需要的便签组合： self.y = self.y[:, [任务预测标签所在的列索引, 受试者ID所在的列索引]] (注意：该项目强制规定最后产出的 y 应为 [预测标签, 受试者ID])
3. 注册新数据集
